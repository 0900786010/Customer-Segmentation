# Customer-Segmentation
🧩 Customer Segmentation  A machine learning project that uses K-Means clustering (with PCA) to group customers based on behavior and demographics. Includes preprocessing, clustering, and visual insights for better marketing decisions.
